# 代码签名证书占位文件
# 
# 原始文件：doc/AIStarter.pfx（已从开源仓库中移除）
# 
# 说明：
#   这是 Electron 应用的 Windows 代码签名证书文件。
#   如果你需要发布签名版本，请将你自己的 .pfx 证书文件放到此目录下，
#   并在 package.json 的 build.win.certificateFile 中配置证书路径，
#   在 package.json 的 build.win.certificatePassword 中配置证书密码。
#   建议证书密码通过环境变量注入，不要明文写在 package.json 中。
# 
# 相关配置位置：
#   - package.json → build.win.certificateFile
#   - package.json → build.win.certificatePassword
